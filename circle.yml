machine:
  java:
    version: oraclejdk8
  pre:
    - wget https://github.com/Cnly/Crafter/archive/1.2.1.tar.gz -O crafter.jar
    - tar -xvf crafter.jar
    - cd Crafter-1.2.1/Crafter
    - mvn clean install:
        pwd: $HOME/Crafter-1.2.1/Crafter
    - cd ../..
general:
  artifacts:
    - "target/trtest-0.0.1-SNAPSHOT.jar"